Omny Test Reporter

Standalone package extracted from the omnyflow SDK to run, parse and report Vitest failures.

CLI Usage:

- Install globally or link locally: `npm link` or `npm i -g .`
- Run reporter: `omny reporter`

Local Usage:

- `npm install`
- `npm start` (runs `node parse-tests.mjs`)
